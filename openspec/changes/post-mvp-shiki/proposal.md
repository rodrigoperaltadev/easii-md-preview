# Post-MVP Shiki Syntax Highlighting

## Goals

- Highlight fenced code blocks in preview and export using Shiki.
- Match VS Code light/dark theme in preview; use `github-light` for export/print.
- Lazy-load highlighter on first render.

## Non-Goals

- Mermaid, KaTeX, or per-language settings UI in this slice.
