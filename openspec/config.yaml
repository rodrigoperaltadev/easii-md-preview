strict_tdd: false
context: |
  easii-md-preview is a Unclassified software project project.
  Detected markers: none.
  Unit tests run with Vitest (`npm test`) for renderer, sanitize, and export helpers.
  Unit tests: none.
  Integration tests: none.
  E2E tests: none.
rules:
  proposal:
    require_problem_statement: true
  spec:
    require_acceptance_criteria: true
  design:
    require_tradeoffs: true
  tasks:
    protect_review_workload: true
  apply:
    test_command: ""
  verify:
    test_command: ""
testing:
  detected: "2026-05-27"
  runner:
    command: "npm test"
    framework: "vitest"
  layers:
    unit: "npm test"
    integration: ""
    e2e: ""
  commands:
    unit:
      - "npm test"
    integration:
      []
    e2e:
      []
  coverage:
    command: ""
    commands:
      []
quality:
  lint: ""
  lint_commands:
    []
  typecheck: ""
  typecheck_commands:
    []
  format: ""
  format_commands:
    []
