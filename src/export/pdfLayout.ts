/** A4 printable content width at 96 DPI (210mm page − 12mm margins each side). */
export const PDF_CONTENT_WIDTH_PX = 702;

/** Viewport height — only affects initial layout, not page count. */
export const PDF_VIEWPORT_HEIGHT_PX = 1123;
